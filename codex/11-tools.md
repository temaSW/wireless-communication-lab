# Tools

Use `tools/` for repeated mechanical work:

- `tools/theme-page.template.html` is the only HTML template for theme pages.
- `tools/new-theme-page.ps1` creates one page across all four themes.
- `tools/update-cache-version.ps1` updates CSS/JS versions across working HTML files.

Do not manually duplicate theme page boilerplate unless the tools are insufficient.
